# 2026-05-16 — Security Audit (Paused)

## Context
- Upgraded OpenClaw to 2026.5.12 to patch Claw Chain vulnerabilities
- Model switched to Kimi K2.6 (moonshot/kimi-k2.6) — may relate to stability issues during this session
- Session experienced repeated "Something went wrong" crashes on large outputs

## Security Audit Progress

### Phase: Secret Inventory (Incomplete)

**Main config scanned:** `~/.openclaw/openclaw.json`

**Confirmed exposed secrets:**
1. **Telegram bot token** — `channels.telegram.botToken`
2. **Gateway auth token** — `gateway.auth.token`
3. **Gateway auth password** — `gateway.auth.password`
4. **Brave Search API key** — `plugins.entries.brave.config.webSearch.apiKey`
5. **Moonshot API key** — `sk-gQx…YEVp` in `~/.openclaw/agents/main/agent/models.json`

**Files still to inspect:**
- `~/.openclaw/agents/main/agent/auth-profiles.json`
- `~/.openclaw/agents/main/agent/auth-state.json`
- `~/.openclaw/credentials/telegram-pairing.json`
- `~/.openclaw/plugins/installs.json`
- `~/.openclaw/workspace/OpenMontage/config.yaml`
- `~/.openclaw/workspace/OpenMontage/.env.example`

### Next Steps (When Resuming)
1. Read remaining files one at a time (small chunks to avoid crashes)
2. Compile full secret inventory with severity ratings
3. Rotate secrets in priority order
4. Consider moving secrets to environment variables or a secrets manager

## Model Stability Fix (Applied)
- **Issue:** Kimi K2.6 with reasoning enabled crashes on tool calls — `400 thinking is enabled but reasoning_content is missing in assistant tool call message`
- **Fix:** Set `reasoning: false` in `~/.openclaw/agents/main/agent/models.json`
- **Status:** Session now stable

## Notes
- Smaller tool calls work; large outputs trigger crashes
- This is a recurring maintenance concern — document rotation schedule
