# Memory — 2026-07-23

## Kochfoto Image Compositor Skill Created

**What:** Built a structured schema + interaction protocol for image creation tasks.

**Why:** Jonah wants precise controls over what happens and when between interactions when sharing images and instructions. Current workflow works but is haphazard.

**Skill path:** `skills/kochfoto-image-compositor/SKILL.md`

**Key features:**
- `Rendering Task` schema — describes subject, environment, mood, visual controls (composition, lighting, camera, lens, film stock, media type), references, generation params
- `Scene` schema — multi-element compositions with layers, blending, post-process
- **Interaction Protocol** — exact rules for:
  - Images only → classify to asset pool, ask what task
  - Text only → draft schema, ask targeted questions for missing controls
  - Images + text → auto-link, build complete task, confirm before render
  - Reference previous outputs → lookup, reuse, maintain lineage
- Quick-start shorthand — skip full schema with minimal syntax
- Session state tracking — asset pool, tasks, scenes
- File storage under `memory/image-tasks/YYYY-MM-DD/`

**Next:** Test the protocol in a real session. Jonah will likely want to refine the schema after using it.
